@model ProductViewModel;

<div class="flex w-[10rem] flex-col items-center justify-between gap-5 overflow-hidden transition-all duration-700 hover:-translate-y-3 hover:relative">
    <div class="h-full w-full bg-white">

        <!-- make somehow its' children stretch to the full width or sum idk -->
        <!-- the max-w makes it obv take less than the threshold of the value given up until this value  -->
        <!-- the max-w makes it have some spare space on left and right (width) -->
        <div class="flex flex-col gap-3">
            <div class="relative">
                <a class="flex flex-col items-center" asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id">
                    <div class="w-[9rem]">
                        <img class="h-[10rem] w-full rounded-xl" src="@Model.ImageUrl"
                             alt="Product Image" />
                    </div>
                    <div class="absolute bottom-0 flex flex-col items-center gap-0.5 overflow-visible font-medium">
                    @if (Model.OnSale)
                    {
                        <span class="text-xxs bg-pink-600 px-2 py-0.5 text-center text-white">РАЗПРОДАЖБА</span>
                    }
                    @if (Model.Price >= 75)
                    {
                            <span class="text-xxs text-nowrap bg-black px-2 py-0.5 text-center text-white">БЕЗПЛАТНА ДОСТАВКА</span>
                        }
                    </div>
                </a>
            </div>
            <div class="text-center">
                <a asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id">@Model.Name</a>
            </div>
        </div>
        <div class="text-center font-bold">
            <p>@(Model.Price) лв.</p>
        </div>
    </div>
</div>